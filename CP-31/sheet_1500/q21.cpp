#include<bits/stdc++.h>
using namespace std;
#define ll long long

const int M = 1e9 + 7;

int main(){

    int n, k;
    cin >> n >> k;

    // dp[i][last_node] --> number of i length paths that contain only 
    ll ans = 1; // n**(k-2)*(sum)

    return 0;
}

