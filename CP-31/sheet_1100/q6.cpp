#include<iostream>
#include<bits/stdc++.h>
using namespace std;

int main(){

    int t;
    cin >> t;

    while(t--){
        int n, q;
        cin >> n >> q;
        vector<bool> visited(30, false);

        vector<int> arr(n, 0);

        for (int i=0; i<n; i++){
            cin >> arr[i];
        }

        
    }

    return 0;
}