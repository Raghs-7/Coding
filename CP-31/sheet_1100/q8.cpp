#include<iostream>
#include<bits/stdc++.h>

using namespace std;

int main(){

    int t;
    cin >> t;

    while(t--){
        int n, k, a, b;
        cin >> n >> k >> a >> b;

        vector<pair<int, int>> points(n, {0, 0});

        for (int i=0; i<n; i++){
            cin >> points[0].first;
            cin >> points[0].second;
        }

        

    }
}