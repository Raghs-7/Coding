# modulus 
// (a * b) % M = ((a % M) * (b % M)) % M
general --> gray code 
dp --> couting tower
dp --> editDistance

// ----------------------------------------------------
// Manacher Algorithm's of string --> all palandromic substrings in O(N)
convert even length palandromes to odd by adding '#' in between them (geek for geek)
        string ms = "@";

        for (char c : s){
            ms += "#" + string(1, c);
        }
        ms += "#";
        ms += "$";

        int n = ms.size();
        
        int l = 0;
        int r = 0;
        vector<int> p(n, 0);
        
        for (int i=1; i<n-1; i++){

            int mirror = (l+r)-i;

            if (i<r){
                p[i] = min(r-i, p[mirror]);
            }

            while(ms[i-1-p[i]] == ms[i+1+p[i]]){
                p[i]++;
            }

            if ( i+p[i] > r){
                l = i-p[i];
                r = i+p[i];
            }
        }
p[i] --> maximum radius around index i;

// -----------------------------------------------
// binary Uplifting ... rather then taking steps 1, 2, 3, 4,... we do binary travel like 13 = 8 + 4 + 1
// complexity reduces to O(n) to O(logn)
// revisit Tree (company Queries I)

// --------------------------------------------------------
// Prefix Sum Uses -->
// let say ur given a path and u want to update yk every node in it by +1 so like It should take O(n) yk to update everyone
// but there's also a way 
// [L, R] --> want to do +100 to everyone so 
// arr[L] = +100 and arr[R+1] = -100

// ------------------------------------------------
// -n = ~n + 1;

// -----------------------------------------------
cp-31 (1200 - q20)
// given a string s find the minimum char to remove to make it palandromic --> O(nlogn)

// -----------------------------------------------
// ceil(A/B) = (A+B-1) / B
